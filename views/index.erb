<h1>Word-Definer</h1>

<% if @words.any? %>
  <ul>
    <% @words.each do |word| %>
      <li><a href="/words/<%= word.id %>"><%= word.word %></a></li>
  <% end %>
  </ul>
<% else %>
  <p>We have no words yet, click new and make the first one!</p>
<% end %>

<a href="/words/new">Add a new word!</a>
